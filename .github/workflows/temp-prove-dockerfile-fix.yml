## TEMPORARY — runs the full main build (which builds all artifacts and calls
## application-signals-e2e-test.yml) on this branch, exercising the one-line fix to
## the `upload-main-build-image` unzip step. All-green = fix works. DELETE after.
name: TEMP Prove Dockerfile Fix
on:
  push:
    branches:
      - temp/prove-dockerfile-fix

permissions:
  id-token: write
  contents: read

jobs:
  main-build:
    uses: ./.github/workflows/main-build.yml
    secrets: inherit
    with:
      caller-workflow-name: 'temp-prove-dockerfile-fix'
